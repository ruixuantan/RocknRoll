const devUrl = "http://localhost:8080";
const baseUrl = devUrl + "/api/v1";

export const PATHS = {
  dice: baseUrl + "/dice",
  diceValidator: baseUrl + "/dice/validate",
  stats: baseUrl + "/stats/"
};